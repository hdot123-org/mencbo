{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the panel window",
  "windows": ["panel"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
